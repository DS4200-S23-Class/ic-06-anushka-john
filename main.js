function clickButton() {
    let line = document.getElementById("parallel");
    let color = line.style.stroke
    
    line.style.color;
}

document.getElementById("button1").addEventListener("click", clickButton);
